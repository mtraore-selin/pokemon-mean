import { HoverDialogDirective } from './hover-dialog.directive';

describe('HoverDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverDialogDirective();
    expect(directive).toBeTruthy();
  });
});
